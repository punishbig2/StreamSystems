import styled from 'styled-components';

export const Item = styled.div`
  pointer-events: all;
  label {
    margin: 0;
  }
  font-weight: 600;
  margin: 0 8px 0 0;
  &:last-child {
    margin: 0 0 0 auto;
  }
`;

import { DealEntry, DealType, EntryType } from "types/dealEntry";
import { DealStatus } from "types/dealStatus";
import { isButtonDisabled } from "./buttonStateResolver";

test("New button states", () => {
});

export {};

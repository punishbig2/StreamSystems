import styled from 'styled-components';

export const Layout = styled.div`
  position: absolute;
  transition: left 0.75s, top 0.75s, padding-left 0.25s, padding-right 0.25s;
  box-sizing: border-box;
`;
